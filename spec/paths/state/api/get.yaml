tags:
  - State

security: []
deprecated: false
summary: API
description: |
  Shows available interactions with the platform.
operationId: State_apiState
responses:
  200:
    description: |
      Services are running (not necessarily available).
    content:
      application/json:
        schema:
          type: object
          properties:
            'withdraw':
              type: boolean
              example: true

            'newposition':
              type: boolean
              example: true

            'accountCreation':
              type: boolean
              example: true
